# 系统总体架构

## 宏观分层

Virtual Team 系统分为两大子系统：

```
┌─────────────────────────────────────────────────────────┐
│                    协作应用                              │
│  ┌─────────┐  ┌──────────┐  ┌────────────────────────┐ │
│  │ IM 通讯 │  │ 协作工具  │  │ 组织/员工管理          │ │
│  └─────────┘  └──────────┘  └────────────────────────┘ │
└──────────────────────┬──────────────────────────────────┘
                       │ 协议层
┌──────────────────────┴──────────────────────────────────┐
│                  虚拟员工系统                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │              Agent 服务器                         │  │
│  │  ┌────────────┐  ┌──────────────────────────┐   │  │
│  │  │ 接入层      │  │ 虚拟员工管理服务          │   │  │
│  │  └────────────┘  └──────────────────────────┘   │  │
│  └──────────────────────┬───────────────────────────┘  │
│                         │                               │
│  ┌──────────────────────┴───────────────────────────┐  │
│  │              虚拟员工实例                          │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │  │
│  │  │意图识别   │ │ 主Agent  │ │ 子Agent（动态）   │  │  │
│  │  │Agent     │ │          │ │                  │  │  │
│  │  └──────────┘ └──────────┘ └──────────────────┘  │  │
│  │             VTA Runtime (Agent 基座)              │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                       │ 协议层
┌──────────────────────┴──────────────────────────────────┐
│              工作环境节点 (Work Environment Node)         │
│  ┌──────────┐ ┌──────────┐ ┌─────────┐ ┌────────────┐ │
│  │ MCP Srv  │ │内置工具   │ │第三方    │ │ 沙盒/文件  │ │
│  │          │ │          │ │Agent    │ │ 系统隔离   │ │
│  └──────────┘ └──────────┘ └─────────┘ └────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## 子系统关系

### 协作应用 vs 虚拟员工系统

协作应用是**独立系统**，即使未挂载虚拟员工系统，它仍然可以作为纯粹的即时通讯协作工具运行。虚拟员工系统挂载后，用户才能与虚拟员工通讯——正如真实员工未注册登录时也无法与之对话。

虚拟员工不是协作应用的"内置功能"，而是通过协议**接入**协作应用的外部实体，与用户通过 IM 客户端接入的方式平行。

### 消息流转整体路径

```
用户发消息
  ↓
协作应用（IM 前端）
  ↓
协作应用服务端
  ├── 利用标记/RAG 构建上下文数据段
  └── 转发至 Agent 服务器接入层
        ↓
      Agent 服务器接入层
        ├── 解析消息结构
        ├── 识别目标虚拟员工
        └── 转发至虚拟员工管理服务
              ↓
            虚拟员工管理服务
              ├── 多租户路由
              ├── 恢复/创建虚拟员工实例
              └── 投递消息至虚拟员工
                    ↓
                  虚拟员工内部
                    ├── 意图识别 Agent 分析消息
                    │     ├── 简单消息 → 直接回复
                    │     └── 工作消息 → 创建工作上下文/关联已有
                    ├── 主 Agent 执行工作（需要时）
                    │     ├── 调度工具 → 工作环境节点
                    │     └── 创建子 Agent 处理子任务
                    └── 回复消息 → 沿原路径返回
```

## 关键设计决策

| 决策 | 方案 | 理由 |
|------|------|------|
| 两大子系统分离 | 协作应用 + 虚拟员工系统独立 | 降低耦合，协作应用可独立迭代，虚拟员工系统可独立扩展 |
| 虚拟员工系统内部两层结构 | 接入层 + 管理服务 | 接入层处理协议、管理服务处理生命周期和租户隔离 |
| Agent 基座 | VTA (virtual-teams-agent) | Pure Agent 骨架，零预设，配置包驱动 |
| 虚拟员工内部多 Agent 并行 | 意图 Agent + 主 Agent 独立并行 | 职责清晰，意图识别不阻塞主 Agent 工作 |
| 工具执行远程化 | 工作环境节点 | 适应客户本地环境和云端环境，保持灵活性和安全边界 |
| 多租户隔离 | 租户 = 用户级别 | 一个用户 = 一个独立数据空间 |
